"""Report entry points (local preview of the nightly email + charts).

``training/reports/`` also holds committed report *artifacts* (PNGs, markdown);
this ``__init__`` just makes the preview CLI importable as
``python -m training.reports.generate_latest_report``.
"""
